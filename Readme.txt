An Anatolia MUD area file editor. The runnable jar of the project is included.

Running Instructions:

- Make sure to have a jre version >=15 installed.
- After that, you can run the included "run.jar" using this command:
	java -jar <Path to run.jar>
- Alternatively you should be able to launch the jar app by double clicking.
